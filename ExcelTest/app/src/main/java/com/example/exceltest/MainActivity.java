package com.example.exceltest;import androidx.annotation.NonNull;import androidx.appcompat.app.AppCompatActivity;import androidx.core.app.ActivityCompat;import android.Manifest;import android.content.pm.PackageManager;import android.os.Build;import android.os.Bundle;import android.os.Environment;import android.util.Log;import android.widget.Toast;import java.io.File;import java.util.ArrayList;import java.util.List;public class MainActivity extends AppCompatActivity {    //读写权限    private static String[] PERMISSIONS_STORAGE     = {            Manifest.permission.READ_EXTERNAL_STORAGE,            Manifest.permission.WRITE_EXTERNAL_STORAGE};    //请求状态码    private static int      REQUEST_PERMISSION_CODE = 1;    @Override    protected void onCreate(Bundle savedInstanceState) {                super.onCreate(savedInstanceState);                setContentView(R.layout.activity_main);        if (Build.VERSION.SDK_INT > Build.VERSION_CODES.LOLLIPOP) {            if (ActivityCompat.checkSelfPermission(this, Manifest.permission.WRITE_EXTERNAL_STORAGE) != PackageManager.PERMISSION_GRANTED) {                ActivityCompat.requestPermissions(this, PERMISSIONS_STORAGE, REQUEST_PERMISSION_CODE);            }        }                    // Get the directory for the user's public pictures directory.                    //在 Environment.DIRECTORY_DOWNLOADS 目录下创建名为fileName的文件夹 刷新查看                    File file =                            new File(Environment.getExternalStoragePublicDirectory(Environment.                                    DIRECTORY_DOWNLOADS), "test_0.xls");//参数2是文件名称                    //mkdirs()可以创建多级目录                    //mkdir()只能创建一级目录                    if (!file.mkdir()) {                        Log.e("LOG_TAG", "Directory not created");                        Toast.makeText(MainActivity.this, "目录已存在...", Toast.LENGTH_SHORT).show();                    } else {                        Toast.makeText(MainActivity.this, "创建新目录...", Toast.LENGTH_SHORT).show();                    }                    if (!file.exists()) {                        Toast.makeText(MainActivity.this, "创建新文件...", Toast.LENGTH_SHORT).show();                        file.mkdirs();                    }                    String[] title = {"姓名", "年龄", "男孩"};                    String sheetName = "demoSheetName";                    List<DemoBean> demoBeanList = new ArrayList<>();                    DemoBean demoBean1 = new DemoBean("小李", "10", "男");                    DemoBean demoBean2 = new DemoBean("小张", "12", "女");                    DemoBean demoBean3 = new DemoBean("小王", "18", "男");                    DemoBean demoBean4 = new DemoBean("王香", "13", "女");                    demoBeanList.add(demoBean1);                    demoBeanList.add(demoBean2);                    demoBeanList.add(demoBean3);                    demoBeanList.add(demoBean4);                    ExcelUtil.initExcel(file.getPath(), sheetName, title);                    ExcelUtil.writeObjListToExcel(demoBeanList, file.getPath());                    System.out.println(file.getPath());                }    @Override    public void onRequestPermissionsResult(int requestCode, @NonNull String[] permissions, @NonNull int[] grantResults) {        super.onRequestPermissionsResult(requestCode, permissions, grantResults);        if (requestCode == REQUEST_PERMISSION_CODE) {            for (int i = 0; i < permissions.length; i++) {                Log.i("MainActivity", "申请的权限为：" + permissions[i] + ",申请结果：" + grantResults[i]);            }        }    }}